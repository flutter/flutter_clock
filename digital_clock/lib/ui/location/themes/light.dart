import 'dart:ui';

import 'package:flutter/material.dart';

class Light {
  const Light();

  static Color foreground = const Color(0xFF9C9DAE);

  static Shadow shadow = Shadow(color: Colors.white, offset: Offset(0, 1));
}
